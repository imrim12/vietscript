{
  "name": "@vietscript/sandbox",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "rimraf ./dist && vite build"
  },
  "dependencies": {
    "@vietscript/plugin-vite": "workspace:*",
    "@vietscript/parser": "workspace:*"
  },
  "devDependencies": {
    "@vitest/ui": "catalog:",
    "rimraf": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:"
  }
}

{
  "name": "@davascript/sandbox",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "rimraf ./dist && vite build"
  },
  "dependencies": {
    "@davascript/plugin-vite": "workspace:*",
    "davascript": "workspace:*"
  },
  "devDependencies": {
    "@vitest/ui": "^0.24.5",
    "rimraf": "^3.0.2",
    "typescript": "^4.9.4",
    "vite": "^4.0.4"
  }
}
